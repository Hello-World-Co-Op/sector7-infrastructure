---
apiVersion: v1
kind: Secret
metadata:
  name: appflowy-secrets
  namespace: appflowy
type: Opaque
stringData:
  # PostgreSQL Credentials
  POSTGRES_USER: "degenotterdev"
  POSTGRES_PASSWORD: "G@G!1insight!"
  POSTGRES_DB: "appflowy"

  # JWT Secret (must be at least 32 characters)
  GOTRUE_JWT_SECRET: "sector7-appflowy-jwt-secret-key-32chars-minimum-length-required"

  # GoTrue Admin Credentials
  GOTRUE_ADMIN_EMAIL: "graydon@helloworlddao.com"
  GOTRUE_ADMIN_PASSWORD: "G@G!1insight!"

  # MinIO/S3 Credentials
  APPFLOWY_S3_ACCESS_KEY: "degenotterdev"
  APPFLOWY_S3_SECRET_KEY: "G@G!1insight!"

  # AI/LLM Configuration (Ollama - OpenAI-compatible API)
  APPFLOWY_AI_OPENAI_API_KEY: "ollama"
  APPFLOWY_AI_OPENAI_API_BASE: "http://ollama.ollama.svc.cluster.local:11434/v1"

  # SMTP Configuration (optional - set to enable email, disabled by default)
  GOTRUE_SMTP_HOST: "localhost"
  GOTRUE_SMTP_PORT: "587"
  GOTRUE_SMTP_USER: "none"
  GOTRUE_SMTP_PASS: "none"
  APPFLOWY_MAILER_SMTP_HOST: "localhost"
  APPFLOWY_MAILER_SMTP_PORT: "587"
  APPFLOWY_MAILER_SMTP_USERNAME: "none"
  APPFLOWY_MAILER_SMTP_PASSWORD: "none"
  APPFLOWY_MAILER_SMTP_EMAIL: "noreply@sector7.helloworlddao.com"
