---
apiVersion: v1
kind: Service
metadata:
  name: supabase-db
  namespace: supabase
spec:
  selector:
    app: supabase-db
  ports:
  - port: 5432
    targetPort: 5432
    name: postgres
  clusterIP: None  # Headless service for StatefulSet
